
/**
 * Excluded components or identifiers that should not appear in the final result.
 */
export const EXCLUDED_COMPONENTS = new Set([
    "React.Fragment",
    "React.createElement",
    "React",
]);